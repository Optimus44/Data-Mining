# Raw Data Placement

Raw Stata survey files are intentionally not copied into the final submission repository. Place the `.dta` files here or in a repository-root `stata/` directory when reproducing the analysis locally.

The raw files are read by the workflow and are not required to inspect the included report and generated result artifacts.
